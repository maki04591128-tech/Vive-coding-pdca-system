# 残タスク一覧

**作成日**: 2026-03-05
**最終更新**: 2026-03-05
**対象リポジトリ**: Vive-coding-pdca-system
**分析方法**: ソースコード・テスト・CI/CD・設計書・手順書の包括的レビュー

---

## 概要

現在の実装完成度は **約97%** です。1558件のテストが全てパスし、lint（ruff）・型チェック（mypy）もエラーゼロです。

---

## 📌 ユーザーに確認・判断を求める事項

以下の項目は、**ユーザー（プロジェクトオーナー）の判断・情報提供**が必要です。

### Q1. GUI画面の追加要件

| 項目 | 詳細 |
|------|------|
| **現状** | ダッシュボード（LLMステータス・コスト・ログ表示）のみ実装済み |
| **不足** | PDCA サイクル制御画面、ゴール入力画面、レビュー結果表示画面、監査ログビューア |
| **質問** | どのGUI画面を優先して追加すべきですか？UI/UXのモックアップやワイヤーフレームはありますか？ |

### Q2. i18n（多言語対応）の方針

| 項目 | 詳細 |
|------|------|
| **現状** | `engine/i18n.py` で4言語（日本語・英語・中国語・韓国語）の基盤は実装済み |
| **不足** | 実際の翻訳データセット（各言語のメッセージカタログ）が未作成 |
| **質問** | 多言語対応は必要ですか？必要な場合、対象言語と翻訳データの提供方法は？ |

### Q3. Docker統合テスト・インストーラービルドの優先度

| 項目 | 詳細 |
|------|------|
| **Docker** | `docker/Dockerfile.sandbox` と `docker/whitelist.yml` は存在。統合テストは未実装 |
| **インストーラー** | `installer/build.py` と `installer/downloader.py` は存在。ビルド検証は未実施 |
| **質問** | これらの統合テスト・ビルド検証の優先度はどの程度ですか？ |

### Q4. CI/CDプロバイダーのリポジトリ設定

| 項目 | 詳細 |
|------|------|
| **現状** | CI Adapter の API 呼び出し機能は実装済み（今回追加） |
| **環境変数** | `.env.example` に `GITHUB_TOKEN`、`GITLAB_TOKEN` を追記済み |
| **質問** | 実際のCI連携テストを行う場合、対象リポジトリのオーナー名・リポジトリ名を教えてください |

---

## ✅ 今回対応済みの項目

### 1. CI アダプターの `get_status()` API 実装 ✅

| 項目 | 詳細 |
|------|------|
| **ファイル** | `src/vibe_pdca/engine/ci_adapter.py` |
| **変更内容** | `GitHubActionsAdapter.get_status()` と `GitLabCIAdapter.get_status()` を `httpx` で実装 |
| **認証** | 環境変数（`GITHUB_TOKEN` / `GITLAB_TOKEN`）またはコンストラクタ引数 |
| **エラー処理** | `CIAdapterError` カスタム例外で API エラー・接続エラー・認証エラーを統一処理 |
| **テスト** | 11テスト追加（モック使用でAPI呼び出しを検証） |

### 2. `httpx` 依存の追加 ✅

| 項目 | 詳細 |
|------|------|
| **ファイル** | `pyproject.toml` |
| **変更内容** | `httpx>=0.27` をコア依存に追加 |

### 3. CI/CD環境変数の追記 ✅

| 項目 | 詳細 |
|------|------|
| **ファイル** | `.env.example` |
| **変更内容** | `GITHUB_TOKEN`、`GITLAB_TOKEN` の環境変数テンプレートを追加 |

### 4. プロンプトテンプレート外部化 ✅

| 項目 | 詳細 |
|------|------|
| **ファイル** | `config/prompts/templates.yml`（新規）、`src/vibe_pdca/prompts/__init__.py` |
| **変更内容** | 全8テンプレートをYAML形式で外部化。`PromptBuilder.from_yaml()` クラスメソッド追加。`load_templates_from_yaml()` 関数追加 |
| **後方互換** | ハードコード版のテンプレートはデフォルトのフォールバックとして維持 |
| **テスト** | 5テスト追加（YAML読み込み・エラーハンドリング・実ファイル検証） |

### 5. GUIテストの CI 対応（前回対応済み） ✅

| 項目 | 詳細 |
|------|------|
| **ファイル** | `tests/test_gui.py` |
| **変更内容** | `pytest.importorskip("flet")` で flet 未インストール時に安全にスキップ |

### 6. CIAdapterBase 抽象基底クラス化（前回対応済み） ✅

| 項目 | 詳細 |
|------|------|
| **ファイル** | `src/vibe_pdca/engine/ci_adapter.py` |
| **変更内容** | `ABC` + `@abstractmethod` に変更 |

---

## 🟢 低優先度（改善提案 — 追加情報なしで対応可能）

### 7. config/prompts/ テンプレートファイルの拡充

| 項目 | 詳細 |
|------|------|
| **現状** | `templates.yml` に8テンプレート。JSONスキーマ5件 |
| **対応案** | テンプレートのバリエーション追加（簡潔版・詳細版等） |

### 8. Docker サンドボックスの統合テスト

| 項目 | 詳細 |
|------|------|
| **現状** | `docker/Dockerfile.sandbox` と `docker/whitelist.yml` は存在 |
| **不足** | Docker統合テスト・CI連携テストが未実装 |

### 9. installer/ のビルド検証

| 項目 | 詳細 |
|------|------|
| **現状** | `installer/build.py`、`installer/downloader.py`、`installer/installer_config.yml` が存在 |
| **不足** | インストーラーのビルド・配布テストが未実施 |

---

## テスト実行結果サマリ

```
テスト数:   1558件パス / 0件失敗 / 1件スキップ（GUI）
Lint:       ruff エラー 0件
型チェック: mypy エラー 0件（74ファイル）
```
